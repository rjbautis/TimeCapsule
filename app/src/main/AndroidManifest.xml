<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android121.timecapsule"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/logo_word"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YoutubeActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ViewOpenCapsulesActivity" />
        <activity android:name=".OpenCapsuleActivity" />
        <activity android:name=".OpenActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".CreateCapsuleActivity" />
        <activity android:name=".ContributeActivity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>